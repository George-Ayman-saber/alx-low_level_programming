#include "main.h"

/**
 * main - startpoint
 * Description : this is a c program that print alphabet
 * Return: 0(success)
*/

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
	_putchar('\n');
	return (0);
}
