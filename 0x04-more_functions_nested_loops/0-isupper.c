#include <stdio.h>

/**
 * main - startpoint
 * description: this is a c program the chaeck if c isupper or not
 * Return: 1 (success) 0 (failure)
*/

int main(void)
{
	char c;

	if ( isupper(c))
		return (1);
	else
		return (0)
}
